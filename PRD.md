# Sprout（青禾记账）— 产品需求与决策台账

## 产品定位

- **名称**：青禾记账 / Sprout
- **Slogan**：简洁、高效、实用
- **平台**：Windows / Android
- **存储**：本地 SQLite，无需联网

## 决策记录

| # | 日期 | 决策点 | 结果 |
|---|------|--------|------|
| 1 | 07-31 | 运行平台 | Windows + Android |
| 2 | 07-31 | 数据存储 | 本地 SQLite |
| 3 | 07-31 | 技术栈 | Tauri + React + TypeScript |
| 4 | 07-31 | UI 组件库 | Ant Design 6 |
| 5 | 07-31 | 分类体系 | 10 大类预设 + 自定义 |
| 6 | 07-31 | 英文名 | Sprout（新芽） |
| 7 | 08-01 | 图表库 | Recharts |
| 8 | 08-01 | 布局 | 手机比例（430px）+ 底部导航 |
| 9 | 08-01 | 分类术语 | 分类 / 子分类 |
| 10 | 08-01 | 预设分类 | 锁定不可删改，自定义可操作 |
| 11 | 08-01 | 深色模式 | antd 官方暗色算法 |
| 12 | 08-01 | 中英双语 | 界面 + 分类全翻译 |
| 13 | 08-01 | 导出格式 | CSV（带校验/预览） |
| 14 | 08-01 | 统计下钻 | 折叠列表（饼图纯展示） |
| 15 | 08-01 | 收入功能 | 不做（专注支出） |
| 16 | 08-01 | 测试 | Vitest + 47 个测试用例 |
| 17 | 08-01 | TDD | 红→绿→重构，先写测试再写代码 |
| 18 | 08-01 | 导入架构 | useReducer 状态机（idle→importing→done），零 ref、零 setTimeout hack |
| 19 | 08-01 | 导入去重 | 按 created_at（毫秒级）去重，仅接受青禾6列CSV格式 |
| 20 | 08-01 | 导入去重 Key | 仅 created_at，不做数据内容比对（同日同金额两笔不会被误杀） |
| 21 | 08-01 | 移动端适配 | 标题栏移除、safe-area 适配、键盘行为 adjustResize、环形图+柱状图联动 |
| 22 | 08-01 | 统计性能 | SQL 聚合替代全量加载（9万条8s→0.3s） |
| 23 | 08-01 | 明细分页 | 按日加载 + 前50条分页 + 切月自动选日期 |
| 24 | 08-01 | App 图标 | 用户提供 1024×1024 PNG，覆盖全平台 |
| 25 | 08-01 | 饼图重构 | 实心饼图→环形图，圆心显示总额，小类合并为「其他」 |
| 26 | 08-01 | 导入流程 | 预览→一键导入+结果汇总，错误按分类统计 |
| 27 | 08-01 | 日期限制 | 禁止选择未来日期 |
| 28 | 08-01 | UI 现代化 | 毛玻璃导航栏 / 卡片式列表 / 圆角阴影体系 / 过渡动画 |
| 29 | 08-01 | 测试 | 84 个测试用例 |
| 30 | 08-03 | 点击反馈 | 消除蓝色 tap-highlight 方块，统一 :active 灰底叠加（微信风格），antd 水波纹改为品牌绿 |
| 31 | 08-03 | 金额输入 | 自定义计算器键盘替换系统键盘，支持加减法表达式实时求值 |
| 32 | 08-03 | 收入功能 | 推翻 #15，增加收入记录与收支统计，expenses 表加 type 字段区分支出/收入 |

## 功能清单

### 已完成
- [x] 记一笔：自定义计算器键盘（支持加减法）+ 分类 + 日期 + 备注 + 金额两位小数限制 + 空金额按钮置灰
- [x] 明细：日历视图 + 按日筛选 + 编辑/删除 + 按日加载 + 分页
- [x] 统计：周/月/年 + 环形图 + 柱状趋势图 + 折叠列表 + SQL 聚合
- [x] 自定义分类：预设锁定 + 自定义可增删
- [x] CSV 导出：6列格式 + created_at 毫秒精度 + 制表符防 Excel 截断
- [x] CSV 导入：6列严格校验 + created_at 毫秒去重 + 批量写入 + 进度条 + 结果汇总 + 10MB 上限 + 事务保护
- [x] 中英双语：界面 + 分类 + 日期格式
- [x] 深色模式
- [x] 数据库：自检 + date/category1/created_at 索引
- [x] 移动端适配：safe-area / 键盘 / 返回拦截 / 弹窗位置
- [x] App 图标：Windows/Mac/Android/iOS 全平台
- [x] 调试模式：alert 弹错误 + 清空数据（已隐藏，代码保留）

## 后续开发计划

分四个阶段，按优先级排列。每个条目标注预估复杂度（S: 半天，M: 1-2 天，L: 3-5 天）和涉及层（F=前端，D=数据库，R=Rust）。

---

### Phase 0 —— 基础修复（P0，本周可完成）

> 目的：恢复测试能力，消除已知体验问题，为后续开发铺路。

| # | 任务 | 复杂度 | 层 | 说明 |
|---|------|:--:|:--:|------|
| 0.1 | **修复测试套件** | S | F | 84 个用例因 `@testing-library/jest-dom` 与 vitest 兼容性问题全挂。升级 jest-dom 或改用 vitest 原生断言 |
| 0.2 | **CalculatorInput 纯函数测试** | S | F | `evaluate()` 和 `canAppendDot()` 是纯函数，加 15-20 个用例覆盖所有边界，零成本 |
| 0.3 | **计算器长按退格连删** | S | F | ⌫ 键绑定 `onPointerDown`/`onPointerUp` + `setInterval`，长按 300ms 后连续删除，松手停止 |
| 0.4 | **Android 键盘震动反馈** | S | F | 按键时 `navigator.vibrate(10)`，轻短震动。用 `!!navigator.vibrate` 做能力检测，桌面端自动跳过 |

---

### Phase 1 —— 搜索与筛选（P1，下一个迭代）

> 目的：解决"找不到旧账"的核心痛点。当前只支持日历逐月翻。

| # | 任务 | 复杂度 | 层 | 说明 |
|---|------|:--:|:--:|------|
| 1.1 | **全局搜索栏** | M | F+D | 明细页顶部加搜索框，关键词匹配备注和分类名。SQL 端用 `LIKE '%keyword%'`，前端防抖 300ms |
| 1.2 | **搜索结果视图** | M | F | 搜索结果按日期分组展示，高亮匹配文字，点击跳转到对应日期的明细位置 |
| 1.3 | **高级筛选** | M | F+D | 按分类（多选）、金额区间（min-max）、日期范围组合筛选。SQL 动态拼接 WHERE 子句 |
| 1.4 | **筛选快捷操作** | S | F | "本月餐饮" "上月交通" 等常用筛选一键直达。基于使用频率排前 5 个 |
| 1.5 | **搜索无结果引导** | S | F | 空结果页面引导用户检查筛选条件或创建新记录 |

---

### Phase 2 —— 收入功能（P2）

> 目的：推翻决策 #15，从纯支出记账升级为收支全记录，覆盖更完整的个人财务场景。

| # | 任务 | 复杂度 | 层 | 说明 |
|---|------|:--:|:--:|------|
| 2.1 | **DB Schema 迁移** | M | D | `expenses` 表加 `type TEXT NOT NULL DEFAULT 'expense'`。可选：重命名表为 `transactions`。向下兼容：旧数据默认 type='expense'，无需回填 |
| 2.2 | **收入分类体系** | M | F+D | 新增预设收入分类：工资收入、奖金收入、投资收益、兼职副业、退款报销、礼金红包、其他收入。约 7 个一级分类，无需二级分类（收入来源简单）。和支出分类分开展示 |
| 2.3 | **记一笔收支切换** | M | F | RecordFlow 顶部加 Segmented 控件：[支出 | 收入]。选收入时：分类切换为收入分类列表、金额前缀 ¥ 变绿色→蓝色、按钮文案"记账"→"记收入"。CalculatorInput 复用无变化 |
| 2.4 | **明细列表区分** | M | F | 收支记录共用一个列表，支出金额红色前缀 `-¥`，收入金额绿色/蓝色前缀 `+¥`。日历标记点区分颜色（支出绿点、收入蓝点） |
| 2.5 | **统计页收支对比** | L | F | 月度汇总卡片拆为：支出 ¥X / 收入 ¥Y / 结余 ¥Z。柱状趋势图叠加收入/支出双色柱。分类统计默认展示支出，可切换查看收入构成。环形图中心显示结余而非支出总额 |
| 2.6 | **CSV 格式升级** | M | F | 6 列 → 7 列，新增 `类型` 列（支出/收入）。导入兼容旧 6 列格式（无类型列默认视为支出）。导出新版本用 7 列 |
| 2.7 | **数据一致性处理** | S | D | 编辑记录时 type 不可更改（支出不能改成收入，反之亦然。如需修改 → 删除重建）。确保统计口径一致 |

---

### Phase 3 —— 预算管理（P3）

> 目的：从"记流水"升级到"控支出"，增加用户粘性。

| # | 任务 | 复杂度 | 层 | 说明 |
|---|------|:--:|:--:|------|
| 3.1 | **预算数据模型** | M | D | 新表 `budgets`：`id, year_month, category1, amount, created_at`。category1 可为 NULL 表示总额预算。仅针对支出分类 |
| 3.2 | **预算设置 UI** | M | F | Profile 页新增"预算管理"入口。表单：选择月份 + 可选分类 + 金额。可复制上月预算 |
| 3.3 | **预算使用展示** | L | F | 统计页月度支出卡片旁边显示预算进度条。分类柱状图上叠加预算线。超 80% 黄色，超 100% 红色 |
| 3.4 | **超支提醒** | M | F | 记一笔后如果当月支出总额/该分类超预算，toast 提示"本月餐饮已用 850/800"。首页卡片加预算进度环 |
| 3.5 | **预算报告** | M | F | 月度预算 vs 实际对比表，超支分类红色高亮，节余分类绿色。可导出为 CSV |

---

### Phase 4 —— 体验打磨（P4）

> 目的：提升视觉品质和操作流畅度，接近原生 App 手感。

| # | 任务 | 复杂度 | 层 | 说明 |
|---|------|:--:|:--:|------|
| 4.1 | **页面切换动画** | M | F | 底部导航切换时左右 slide 过渡（`translateX` 200ms），方向跟随标签位置 |
| 4.2 | **统计数字跳动** | S | F | 月度总额、笔数从 0 跳到实际值，`requestAnimationFrame` + easeOut 缓动，持续 600ms |
| 4.3 | **删除滑出动画** | S | F | 明细行左滑露出删除按钮（或直接滑出消失 + 底部 undo toast）。iOS 邮件风格 |
| 4.4 | **记一笔成功反馈** | S | F | 提交成功 → 按钮短暂变绿 ✓ + 金额数字弹跳一下，替代纯 toast |
| 4.5 | **空状态插画** | M | F | 无记录月份、无搜索结果、无统计数据等场景加 emoji + 引导文案（已有部分，补全） |
| 4.6 | **最近金额快捷复用** | S | F | 计算器键盘上方显示最近 3 次输入的金额，点击一键填入。LocalStorage 存储 |
| 4.7 | **下拉刷新** | S | F | 明细页和统计页下拉拉回刷新，移动端标准手势 |

---

### Phase 5 —— 发布与基础设施（P5）

> 目的：为正式分发和长期维护做准备。

| # | 任务 | 复杂度 | 层 | 说明 |
|---|------|:--:|:--:|------|
| 5.1 | **Android keystore 签名** | M | R | `keytool -genkey` 生成 release keystore，配置 `tauri.conf.json` signingConfig，GitHub Actions 注入 |
| 5.2 | **备份提醒智能化** | M | F | 现有提醒逻辑升级：超过 7 天未备份 + 新增 50 条以上记录才触发。不再仅按时间固定提醒 |
| 5.3 | **自动备份选项** | M | F+R | 可配置的定时间隔自动导出到指定目录。Tauri 端用 `tauri-plugin-fs` + 定时器 |
| 5.4 | **数据统计看板** | M | F | Profile 页加数据概览：总记录数、最早/最新日期、数据库文件大小。纯统计展示 |
| 5.5 | **窗口置顶** | S | R | Tauri 窗口 API `setAlwaysOnTop(true)`，Profile 页加开关。仅桌面端有效 |
| 5.6 | **错误日志收集** | M | F+R | `console.error` 和未捕获异常写入日志文件，Profile 页可查看/导出。仅 debug 模式下暴露 |

---

### 远景（暂不排期）

| 功能 | 说明 |
|------|------|
| 周期账单 | 房租、订阅等每月自动生成支出记录，减少重复输入 |
| 多账本 | 旅行账本、家庭账本等独立空间，切换隔离 |
| 桌面小组件 | Android 主屏幕显示当月总支出，点击跳转记一笔 |
| 数据看板 | 年度报告：总支出趋势、分类占比变化、消费习惯标签 |
| 云同步 | WebDAV 或自定义服务器同步，多设备共享数据（会打破"无需联网"定位） |

---

### 进度总览

```
Phase 0  ░░░░░░░░░░  0%  基础修复
Phase 1  ░░░░░░░░░░  0%  搜索与筛选
Phase 2  ░░░░░░░░░░  0%  收入功能
Phase 3  ░░░░░░░░░░  0%  预算管理
Phase 4  ░░░░░░░░░░  0%  体验打磨
Phase 5  ░░░░░░░░░░  0%  发布与基础设施
```

---

## 架构分析：收入功能支持度

> 评估当前代码架构是否支持增加收入记录，以及需要改动的范围。

### 当前状态

代码库**完全为纯支出设计**——数据模型、SQL 聚合、UI 组件、CSV 格式、分类体系均无"交易类型"概念。

| 层级 | 当前实现 | 收入支持度 |
|------|---------|:--:|
| 数据模型 | `Expense` 接口无 `type` 字段，`expenses` 表无类型列（[db.ts:8-16](src/db.ts#L8-L16)） | ✗ |
| SQL 聚合 | `getStatsSummary`/`getStatsByCategory`/`getStatsTrend` 全部无类型过滤，`SUM(amount)` 会混合收支（[db.ts:348-400](src/db.ts#L348-L400)） | ✗ |
| 分类体系 | 10 个预设分类全是支出类别，无收入分类（[categories.ts:9-13](src/data/categories.ts#L9-L13)） | ✗ |
| 表单 | RecordFlow 无收支切换，仅 `amount > 0` 校验（[RecordFlow.tsx:56](src/components/RecordFlow.tsx#L56)）；`¥` 前缀在明细页渲染 | ✗ |
| 明细列表 | 金额前缀硬编码 `-¥`（[ExpenseList.tsx:201](src/components/ExpenseList.tsx#L201)），日合计直接累加不区分类型 | ✗ |
| 统计页 | 卡片标题固定"总支出"，环形图+趋势图全按支出聚合（[MonthlyStats.tsx](src/components/MonthlyStats.tsx)） | ✗ |
| CSV 导出 | 6 列格式：日期,一级分类,二级分类,金额,备注,记录时间。无类型列 | ✗ |
| CSV 导入 | 按固定位置索引解析（c[3]=金额），6 列硬校验，无类型兼容（[Profile.tsx:108-126](src/components/Profile.tsx#L108-L126)） | ✗ |
| i18n | 无收入/结余等文案（[translations.ts](src/i18n/translations.ts)） | ✗ |

### 改动范围

收入功能是一个**全栈穿透性改动**，涉及 DB Schema 迁移 + 所有 SQL 查询 + 所有 UI 组件 + CSV 格式 + i18n。

| 层级 | 改动文件 | 改动量 | 说明 |
|------|---------|:--:|------|
| 数据库 | `db.ts` | 大 | `expenses` 表加 `type` 列（默认 `'expense'`）；新增收入默认分类；`batchAddExpenses` 参数偏移量从 6→7；~10 个聚合查询加 `WHERE type` 过滤；新增收入聚合函数 |
| 分类 | `categories.ts`, `db.ts` | 中 | 新增 7 个收入一级分类；`categories1` 表加 `kind` 列区分收支；分类翻译补全 |
| 表单 | `RecordFlow.tsx` | 中 | 顶端加 Segmented [支出｜收入] 切换；分类列表按类型过滤；金额前缀 ¥ 颜色跟随类型变化 |
| 明细 | `ExpenseList.tsx` | 中 | 金额渲染区分 `+¥`/`-¥` 和颜色；日/月合计拆支出+收入+结余；编辑弹窗加类型字段 |
| 统计 | `MonthlyStats.tsx` | 大 | 汇总卡片拆为支出/收入/结余三栏；环形图可切换收支视图；趋势图双色叠加柱；分类统计按类型切换 |
| CSV | `Profile.tsx`, `translations.ts` | 中 | 6 列→7 列，新增"类型"列；导入兼容旧 6 列格式（无类型默认支出）；索引位置全部 +1 |
| i18n | `translations.ts`, `categoryTranslations.ts` | 小 | 新增收入/结余相关中英文案 |
| 测试 | 多个测试文件 | 中 | `import-csv.test.ts`, `stats-calc.test.ts` 等适配新格式 |

### 关键设计决策

1. **表名保持 `expenses`**，不重命名为 `transactions`。重命名涉及所有 SQL 语句、接口名、函数名和组件引用，风险远大于收益。加一个 `type` 列即可，语义上"expense 表记录所有交易"可以接受。

2. **type 字段值**：`'expense'` / `'income'`（字符串），不用布尔。扩展性更好——未来可能加 `'transfer'`（转账）。

3. **分类表加 `kind` 列**（`'expense'` / `'income'`），而非收支分类混在一起。这样表单切换收支时只需过滤分类列表，不需要维护两份完全独立的分类树。

4. **金额始终存正数**，靠 `type` 区分方向。不用 `-100` 表示支出的符号约定——统计聚合时容易因为疏漏导致 SUM 值错误。

5. **CSV 向后兼容**：导入时如果只有 6 列 → 默认 `type='expense'`。导出始终 7 列。

---

## 架构分析：用户管理与云同步可行性

> 评估从"纯本地"到"多用户 + 云端"的技术路径。

### 用户管理

**当前状态：** 零认证基础设施。无登录、无账号、无权限。Tauri 是桌面/移动端本地运行时，不内置用户系统。

**可行性：** 需要自建或接入完整的后端服务。

| 方案 | 做法 | 复杂度 | 适合场景 |
|------|------|:--:|------|
| A. 本地多用户 | 应用内切换 profile，每个 profile 有独立 SQLite 文件。无需服务端 | 低 | 多人共用一台设备（如家庭平板） |
| B. 自建后端 | Node.js/Go 服务端 + PostgreSQL + JWT 认证 + REST API | 高 | 完全可控，但运维成本大 |
| C. 接入 BaaS | Firebase Auth / Supabase Auth，托管的用户认证 | 中 | 快速上线，按量付费，有 vendor lock-in |

**建议路径：** 短期不做用户管理（与"本地优先"定位冲突）。如果未来有"多设备同步"需求 → 先走方案 A（本地多 profile），再考虑方案 C（Supabase 综合成本最低且支持 SQLite 同步）。

### 云数据同步

**当前状态：** 数据存在设备本地 SQLite 文件中。导出/导入是唯一的"跨设备"数据流动方式。无网络层、无同步协议、无冲突处理。

**核心挑战：**

| 挑战 | 说明 |
|------|------|
| 冲突解决 | 两台设备同时改了同一条记录，合并策略是什么？Last-Write-Wins 简单但可能丢数据；CRDT 严谨但实现复杂 |
| 离线优先 | 用户在地铁里记账，出隧道后同步——必须支持离线编辑+自动合并 |
| 数据安全 | 财务数据上云 → 加密传输 + 加密存储 + 用户数据隔离是刚需，不是可选项 |
| 多端一致性 | Windows + Android 数据要同步，表结构变更时需处理跨版本兼容 |

**可选技术路径：**

| 方案 | 做法 | 复杂度 | 优点 | 缺点 |
|------|------|:--:|------|------|
| A. WebDAV 文件同步 | 把 CSV/JSON/SQLite 文件自动上传到 WebDAV 服务器。其他设备拉取合并 | 中 | 无需自建后端；用户可以用坚果云/Nextcloud；保留"本地优先"理念 | 冲突靠人工处理；大文件效率低；不是实时同步 |
| B. SQLite 同步引擎 | 用 [cr-sqlite](https://github.com/vlcn-io/cr-sqlite) 或 [LiteFS](https://fly.io/docs/litefs/) 做数据库级 CRDT 同步 | 高 | 数据库级别自动合并；Tauri 端可嵌入 | 需要 relay 服务器；移动端支持不成熟；调试困难 |
| C. 自建同步后端 | REST/WebSocket API + 服务端数据库 + 自定义冲突策略 | 很高 | 完全可控；可做增量同步 | 开发+运维成本巨大；与"简洁记账"定位不符 |
| D. 导入导出 + 文件同步 | 保持现有 CSV 导出/导入，借用 Syncthing/Resilio 等文件同步工具在设备间同步导出目录 | 极低 | 零开发成本；完全去中心化 | 体验原始；用户需自行配置同步工具 |

**问题不在于能不能做，而在于做完后青禾记账还是不是青禾记账。** 当前的核心卖点是"本地存储·无需联网·数据安全"。云同步会直接削弱这个定位。

**建议路径：** 短期坚持本地定位。如果用户有跨设备需求 → 先做方案 A（WebDAV 自动备份/恢复）作为"增强版导入导出"，保持用户对数据的完全控制权。后续如果市场验证云同步是刚需 → 再评估方案 B。

### 架构演进路线图

```
当前                    Phase 1-5                未来探索
─────                   ──────────               ────────
纯本地 SQLite  ───→  本地多 Profile  ───→  WebDAV 自动备份
单用户                (方案 A)             (云同步方案 A)

                                           ↓ (按需评估)
                                     SQLite CRDT 同步
                                     (云同步方案 B)

                                           ↓ (暂不考虑)
                                     自建后端
                                     (用户管理 + 同步)
```

> 结论：**收入功能完全在当前架构范围内，改动面广但无技术障碍。用户管理和云同步是定位级决策，需要市场验证后再投入。**
